const SortingSectionContainer = ({ children }) => {
    return (
        <div className="hidden md:flex p-10 mt-24 w-screen justify-between bg-white">
            {children}
        </div>
    )
}

export default SortingSectionContainer